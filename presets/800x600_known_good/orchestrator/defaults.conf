# Orchestrator defaults — known-good 800x600 safe startup state.
# Pack Opener: enabled. Market/Figurines: disabled until calibrated.
REFRESH_INTERVAL_MINUTES=10
PACK_OPENER_ENABLED=1
MARKET_BUYER_ENABLED=0
FIGURINES_BUYER_ENABLED=0
STAR_TRIALS_ENABLED=0
# Estimated bundles placed per pack opener cycle.
BUNDLES_PER_CYCLE=13
