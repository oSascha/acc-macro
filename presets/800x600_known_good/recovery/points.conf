# Recovery button coordinates — calibrate before enabling recovery.
# These coordinates were not included in the preset because they vary by UI state.
# Set all four values, then set RECOVERY_ENABLED=1 in defaults.conf.
RECOVERY_POPUP_CLOSE_BUTTON_X=
RECOVERY_POPUP_CLOSE_BUTTON_Y=
RECOVERY_ITEMS_TAB_BUTTON_X=
RECOVERY_ITEMS_TAB_BUTTON_Y=
